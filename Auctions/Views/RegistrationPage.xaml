<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Auctions.Views.RegistrationPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Back" Clicked="OnBack"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15">
            <Label Text="First name" FontSize="Medium" />
            <Entry Text="{Binding FirstName, Mode=TwoWay}" FontSize="Medium" />

            <Label Text="Last name" FontSize="Medium" />
            <Entry Text="{Binding LastName, Mode=TwoWay}" FontSize="Medium" />
             
            <Label Text="User name" FontSize="Medium"/>
            <Entry Text="{Binding UserName, Mode=TwoWay}" FontSize="Medium"/>

            <Label Text="Birth date" FontSize="Medium"/>
            <DatePicker Date="{Binding DataNasterii, Mode=TwoWay}"/>
            
            <Label Text="Email" FontSize="Medium"/>
            <Entry Text="{Binding Email, Mode=TwoWay}" FontSize="Medium"/>

            <Label Text="Password" FontSize="Medium"/>
            <Entry Text="{Binding Password, Mode=TwoWay}" FontSize="Medium" IsPassword="True"/>
            
            <StackLayout Orientation="Horizontal">
                <Button Text="Register" Clicked="OnRegister" HorizontalOptions="FillAndExpand"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
